# MockGen AI Environment Variables
# Copy this file to .env and fill in your actual values
# Only sensitive information is managed via environment variables

# ============================================================================
# General Configuration
# ============================================================================

# Default URL (OPTIONAL)
# If set, MockGen AI will skip the URL input step and use this URL directly
# Example: https://global.oliveyoung.com
MOCKGEN_DEFAULT_URL=

# ============================================================================
# AI Configuration
# ============================================================================

# Google Gemini API Key (REQUIRED)
# Get your API key from: https://makersuite.google.com/app/apikey
AI_API_KEY=your-actual-api-key-here

# ============================================================================
# Network Request Filtering
# ============================================================================

# Allowed Hosts (OPTIONAL)
# Comma-separated list of host names for network request filtering
# Only requests to these hosts will be captured and processed
# Example: api.example.com,staging-api.example.com,localhost:3000
MOCKGEN_ALLOWED_HOSTS=api.example.com,staging-api.example.com,localhost:3000

# ============================================================================
# Usage Instructions
# ============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and set your actual values:
#    - Replace "your-actual-api-key-here" with your real Gemini API key
#    - Adjust other settings as needed
#
# 3. Environment variables will override config.yaml settings
#
# 4. Never commit .env file to version control (it's in .gitignore)
#
# ============================================================================
# Security Notes
# ============================================================================
#
# - Keep your API key secure and don't share it
# - Never commit .env file to version control
# - Use different .env files for different environments
# - Regularly rotate your API keys for security
#
# ============================================================================